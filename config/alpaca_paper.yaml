# Alpaca Paper broker non-secret configuration.
#
# Credentials (api key, secret) are NEVER stored here. They are read
# from environment variables:
#   ALPHABRIEF_ALPACA_KEY
#   ALPHABRIEF_ALPACA_SECRET
# Historical config pending deletion in blueprint M01. Never store keys here.
base_url: https://paper-api.alpaca.markets
request_timeout_seconds: 5.0
max_order_attempts: 3
retry_backoff_seconds: 0.25
